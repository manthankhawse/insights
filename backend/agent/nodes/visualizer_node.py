from agent.state import AgentState

def generate_visuals(state: AgentState):
    """Pass-through node for now. Will generate charts later."""
    print("📊 [Visualizer] Passing SQL results to frontend...")
    return {} # No state changes, just passes the ui_blocks forward